# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Step 5 contains inline questions and WAITs that allow skipping license info
  - **CRITICAL**: This test MUST FAIL on unfixed code — failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior — it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate Step 5 of `module-02-sdk-setup.md` contains inline questions with WAIT instructions and a conditional structure that allows the agent to skip presenting license options
  - **Scoped PBT Approach**: Scope the property to the single affected file, Step 5 section only
  - Parse `senzing-bootcamp/steering/module-02-sdk-setup.md` and extract the Step 5 (Configure License) section
  - Assert Step 5 does NOT contain WAIT instructions (`WAIT for response`)
  - Assert Step 5 does NOT contain inline closing questions (e.g., "Do you have a Senzing license file or BASE64 key?", "Use this one?")
  - Assert Step 5 contains unconditional instructions to present license information as content:
    - Evaluation license limits (500 records) presented as informational content
    - Custom license instructions (how to provide a file or BASE64 key) presented as content
    - License acquisition details (support@senzing.com, sales@senzing.com) presented as content
  - `isBugCondition(step5)`: step5 CONTAINS inline questions with WAIT instructions AND uses conditional structure that allows skipping license info
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct — it proves Step 5 has inline questions/WAITs and gates info behind conditionals)
  - Document counterexamples found
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - License mechanics, other steps, and hook behavior unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - **Step 1 — Observe on UNFIXED code:**
    - Extract Step 5 (Configure License) and record the license check priority order sentence
    - Record the BASE64 decode command (`echo 'YOUR_BASE64_KEY' | base64 --decode > licenses/g2.lic`)
    - Record the "NEVER ask the user to paste a license key into chat" warning
    - Record the engine config PIPELINE `LICENSEFILE` JSON block
    - Record the `config/bootcamp_preferences.yaml` recording instructions (`license: custom` or `license: evaluation`)
    - Record the license acquisition contacts (support@senzing.com, sales@senzing.com)
    - Record the checkpoint write instruction for Step 5
    - Record all other steps (Steps 1–4, 6–9) content in full
    - Record the step heading sequence for the entire file
  - **Step 2 — Write property-based tests asserting observed behavior is preserved:**
    - Property: Step 5 still contains the license check priority order (project-local → env var → system CONFIGPATH → built-in evaluation)
    - Property: Step 5 still contains the BASE64 decode command
    - Property: Step 5 still contains the "NEVER ask the user to paste a license key into chat" warning
    - Property: Step 5 still contains the engine config PIPELINE `LICENSEFILE` JSON
    - Property: Step 5 still contains `license: custom` and `license: evaluation` preference recording
    - Property: Step 5 still contains license acquisition contacts
    - Property: Step 5 still contains the checkpoint write instruction
    - Property: all other steps (1–4, 6–9) are identical between unfixed and fixed versions
    - Property: the step heading sequence for the entire file is unchanged
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 3. Restructure Step 5 to present license information as content

  - [x] 3.1 Rewrite Step 5 in `senzing-bootcamp/steering/module-02-sdk-setup.md`
    - **File**: `senzing-bootcamp/steering/module-02-sdk-setup.md`
    - **Section**: `## Step 5: Configure License`
    - **Keep unchanged:**
      - Opening sentence about Senzing license check priority order
      - Instruction to check all three locations
      - The 🚨 "NEVER ask the user to paste a license key into chat" warning
      - The BASE64 decode command block
      - The engine config PIPELINE `LICENSEFILE` JSON block
      - The `config/bootcamp_preferences.yaml` recording instructions
      - License acquisition contact information (support@senzing.com, sales@senzing.com)
      - The `licenses/README.md` reference
      - The checkpoint write instruction
    - **Remove:**
      - Inline question: `"I found a license at [location]. Use this one?"` and its `WAIT for response`
      - Inline question: `"Do you have a Senzing license file or BASE64 key? If not, the SDK works with evaluation limits (500 records)."` and its `WAIT for response`
      - The conditional ask structure that gates information behind questions
    - **Add/restructure:**
      - Instruct the agent to always present license information as content regardless of check results
      - If a license is found: present the finding as informational content (location, details)
      - If no license is found: present all options as informational content:
        - Evaluation license: works with 500-record limit, no file needed
        - Custom license: how to provide a license file or decode a BASE64 key
        - License acquisition: where to get evaluation and production licenses
      - The agent presents this information and stops — the `ask-bootcamper` hook generates the closing question
    - **Do NOT modify** any other step (Steps 1–4, 6–9) or any other section of the file
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

  - [x] 3.2 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Step 5 presents license info as content without inline questions/WAITs
    - **IMPORTANT**: Re-run the SAME test from task 1 — do NOT write a new test
    - The test from task 1 encodes the expected behavior (no inline questions or WAITs in Step 5, license info presented as unconditional content)
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.3 Verify preservation tests still pass
    - **Property 2: Preservation** - License mechanics, other steps, and hook behavior unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 — do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 4. Checkpoint - Ensure all tests pass
  - Run the full test suite (bug condition + preservation tests)
  - Ensure all tests pass, ask the user if questions arise
