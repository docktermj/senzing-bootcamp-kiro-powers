# Implementation Plan

## Task Dependency Graph

```
1 (Bug Condition Exploration Test)
│
2 (Preservation Property Tests)
│
3 (Implement Fix)
├── 3.1 Edit session-resume.md
├── 3.2 Edit module-completion-track.md
├── 3.3 Edit agent-behavior-rules.md
├── 3.4 Edit graduation.md
├── 3.5 Verify exploration test passes
└── 3.6 Verify preservation tests pass
│
4 (Checkpoint)
```

Tasks 1 and 2 run on UNFIXED code. Task 3 implements the fix and verifies both test suites pass after.

---

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Question Format Missing at Session-Recreation and Track-Completion Paths
  - **CRITICAL**: This test MUST FAIL on unfixed code — failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior — it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the formatting guidance gaps in unfixed steering files
  - **Scoped PBT Approach**: Scope the property to the concrete failing cases — parse the four target steering files and assert required content patterns
  - Test file: `tests/test_question_format_consistency_bug.py`
  - Test that `senzing-bootcamp/steering/session-resume.md` Step 3 contains an explicit instruction to re-render pending questions with the standard pointer+bold format when re-presenting across a session boundary
  - Test that `senzing-bootcamp/steering/module-completion-track.md` contains an explicit, unconditional bootcamp-completion closing question subsection with pointer prefix and bold formatting
  - Test that `senzing-bootcamp/steering/agent-behavior-rules.md` Rule 4 contains a "Session-Recreation Re-Rendering" clause
  - Test that `senzing-bootcamp/steering/agent-behavior-rules.md` Rule 4 contains a "Track-Completion / Graduation Terminal Turn" clause
  - Test that `senzing-bootcamp/steering/graduation.md` Mandatory Closing Step contains a pointer+bold formatted closing question
  - Use Hypothesis to generate random question texts and verify the re-rendering instruction format produces the standard pointer+bold pattern for any stored pending question content
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct — it proves the formatting guidance gaps exist)
  - Document counterexamples found (e.g., "session-resume.md Step 3 has no re-rendering instruction", "module-completion-track.md has no unconditional closing pointer question")
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Non-Target Steering Files and Paths Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Test file: `tests/test_question_format_consistency_preservation.py`
  - Observe: Non-target steering files (onboarding, module steps, in-session resume welcome-back, informational content) already have correct pointer+bold formatting or correctly omit pointer
  - Observe: `session-resume.md` sections OTHER than the pending-question re-presentation path are unchanged
  - Observe: `module-completion-track.md` sections OTHER than the closing-question subsection are unchanged
  - Observe: `graduation.md` sections OTHER than the Mandatory Closing Step terminal question are unchanged
  - Observe: `agent-behavior-rules.md` Rule 4 existing clauses are present and unchanged
  - Write property-based test: for all non-target steering files in `senzing-bootcamp/steering/`, file content is byte-identical before and after the fix (snapshot comparison)
  - Write property-based test: for random non-target paths generated by Hypothesis, verify no formatting changes introduced outside the two target paths (session-recreation re-presentation and graduation/track-completion closing)
  - Write structural assertion: `.question_pending` lifecycle rules (write-policy-gate, treat-as-answer, delete-and-process) remain referenced and unchanged in the steering files
  - Verify tests pass on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 3. Fix for question format consistency at session-recreation and track-completion paths

  - [x] 3.1 Edit `senzing-bootcamp/steering/session-resume.md` — Add Pending Question Re-Rendering subsection
    - Add a "Pending Question Re-Rendering" subsection to Step 3 (Summarize and Confirm)
    - Instruct the agent: when `config/.question_pending` exists from a prior session, read the stored question text (lines 2+), re-render it with the standard pointer prefix and bold formatting before presenting
    - Specify the format explicitly: pointer at start of line, outside bold span, question text inside bold markers
    - State that the re-rendered question replaces the default "Ready to continue?" question so only one pointer question closes the turn
    - Preserve the `.question_pending` lifecycle: file remains in place, treat-as-answer and delete-and-process rules still apply on the bootcamper's next response
    - _Bug_Condition: isBugCondition(input) where input.isSessionResumeTurn AND input.pendingQuestionExists AND input.isNewSession AND NOT input.pendingQuestionRenderedWithPointerAndBold_
    - _Expected_Behavior: Re-presented pending question carries pointer prefix and bold formatting_
    - _Preservation: .question_pending lifecycle unchanged; same-session re-presentation unaffected_
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.2 Edit `senzing-bootcamp/steering/module-completion-track.md` — Add Bootcamp-Completion Closing Question subsection
    - Add a "Bootcamp-Completion Closing Question" subsection at the end of the Path Completion Celebration flow
    - Place it after all offers are resolved and before loading `lessons-learned.md`
    - Define the unconditional final question using pointer+bold format (e.g., pointer followed by bold "The Senzing Bootcamp is complete. Do you have anything else you would like to discuss?")
    - State that pointer is at start of line, outside the bold span; any celebratory emoji appears before or within bold but does not replace pointer
    - Enforce the One Question Rule for this terminal turn — exactly one pointer question, written to `config/.question_pending`, agent stops immediately after
    - _Bug_Condition: isBugCondition(input) where input.isTrackCompletionTurn AND (NOT input.closingQuestionExists OR NOT input.closingQuestionHasPointerPrefix)_
    - _Expected_Behavior: Track-completion turn ends with exactly one pointer+bold closing question_
    - _Preservation: All other celebration content (offers, recap, artifacts) unchanged_
    - _Requirements: 2.4, 2.5, 2.6_

  - [x] 3.3 Edit `senzing-bootcamp/steering/agent-behavior-rules.md` — Add two clauses to Rule 4
    - Add a "Session-Recreation Re-Rendering" clause to Rule 4: "When re-presenting a pending question stored in `config/.question_pending` after a session boundary (new session creation), re-render the stored question text with the pointer prefix and bold formatting. Do not echo raw stored text without presentational formatting."
    - Add a "Track-Completion / Graduation Terminal Turn" clause to Rule 4: "The track-completion and graduation-final turns are subject to the same pointer+bold formatting convention as all other yielding turns. The terminal question closing the bootcamp carries the pointer prefix regardless of any celebratory emoji present."
    - _Bug_Condition: Rule 4 does not address session-recreation re-rendering or track-completion terminal turns_
    - _Expected_Behavior: Rule 4 explicitly covers both paths_
    - _Preservation: Existing Rule 4 clauses and all other rules unchanged_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6_

  - [x] 3.4 Edit `senzing-bootcamp/steering/graduation.md` — Ensure Mandatory Closing Step ends with pointer question
    - In the "Mandatory Closing Step" section (end of graduation), ensure the post-graduation announcement ends with exactly one pointer+bold question
    - Make the requirement explicit so the agent cannot omit the pointer from the graduation-final question
    - Example format: pointer followed by bold "Is there anything else you would like to discuss or explore?"
    - _Bug_Condition: isBugCondition(input) where input.isGraduationFinalTurn AND NOT input.closingQuestionHasPointerPrefix_
    - _Expected_Behavior: Graduation-final turn ends with pointer+bold closing question_
    - _Preservation: All other graduation artifacts (recap, PDF, transcript, certificate, indexes, report) unchanged_
    - _Requirements: 2.4, 2.5, 2.6_

  - [x] 3.5 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Question Format Present at Session-Recreation and Track-Completion Paths
    - **IMPORTANT**: Re-run the SAME test from task 1 — do NOT write a new test
    - The test from task 1 encodes the expected behavior (presence of re-rendering instruction, closing question, Rule 4 clauses)
    - When this test passes, it confirms the expected behavior is satisfied across all four steering files
    - Run: `pytest tests/test_question_format_consistency_bug.py -v`
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6_

  - [x] 3.6 Verify preservation tests still pass
    - **Property 2: Preservation** - Non-Target Steering Files and Paths Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 — do NOT write new tests
    - Run: `pytest tests/test_question_format_consistency_preservation.py -v`
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all preservation tests still pass after fix (no regressions to non-target paths)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 4. Checkpoint - Ensure all tests pass
  - Run full test suite: `pytest tests/test_question_format_consistency_bug.py tests/test_question_format_consistency_preservation.py -v`
  - Ensure all tests pass, ask the user if questions arise.
  - Verify no other tests in the repo are broken by the steering file edits: `pytest tests/ -v`
