# Tasks: Rename "path" to "track"

## Task 1: Update onboarding-flow.md

- [x] 1.1 Replace "path selection" with "track selection" in the header description (line 7) and strict rule note (line 9)
- [x] 1.2 Replace "Paths let you skip" with "Tracks let you skip" in the overview bullet
- [x] 1.3 Replace "before we choose a path" with "before we choose a track" in the pre-selection prompt
- [x] 1.4 Rename section heading `## 5. Path Selection` to `## 5. Track Selection`
- [x] 1.5 Replace "Present paths" with "Present tracks" and "Which path sounds right" with "Which track sounds right"
- [x] 1.6 Rename section heading `## Switching Paths` to `## Switching Tracks` and replace "path requirements" with "track requirements" in its body

## Task 2: Update POWER.md

- [x] 2.1 Replace `learning-path` with `learning-track` in the YAML frontmatter keywords
- [x] 2.2 Replace "Choose your path" with "Choose your track" in the Quick Start section
- [x] 2.3 Replace "Full learning path" with "Full learning track" in option C description
- [x] 2.4 Replace "Paths are not mutually exclusive" with "Tracks are not mutually exclusive" and "Path A"/"Path C" with "Track A"/"Track C"
- [x] 2.5 Replace "path selection" with "track selection" in the onboarding-flow.md steering file description
- [x] 2.6 Replace "path completion celebration" with "track completion celebration" in the module-completion.md description

## Task 3: Update agent-instructions.md

- [x] 3.1 Replace "At path end" with "At track end" in the Module Steering section
- [x] 3.2 Replace "path-completion workflow" with "track-completion workflow" in the module transitions line

## Task 4: Update session-resume.md

- [x] 4.1 Replace "chosen language, path, cloud provider" with "chosen language, track, cloud provider" in the state file description
- [x] 4.2 Replace "Path: [path letter]" with "Track: [track letter]" in the summary template

## Task 5: Verify no learning-option "path" references remain

- [x] 5.1 Run grep across all four files to confirm only file-system "path" occurrences remain
